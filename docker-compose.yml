services:
  mongodb:
    image: mongo:7
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    # Optional: add environment: { MONGO_INITDB_ROOT_USERNAME: dev, MONGO_INITDB_ROOT_PASSWORD: dev }

volumes:
  mongo_data:
